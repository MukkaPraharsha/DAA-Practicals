# conclusion

The Breadth First Search (BFS) algorithm was successfully implemented in Python using an adjacency list and queue. The program accepts the graph and source vertex as user input and traverses the graph level by level. Each vertex is visited only once using a visited array, making the traversal efficient.

Time Complexity: O(V + E)
Space Complexity: O(V)