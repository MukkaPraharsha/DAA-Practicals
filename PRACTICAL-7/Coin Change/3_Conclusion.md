# Conclusion

The Coin Change Problem was successfully implemented using Dynamic Programming. The algorithm calculates the number of ways to make the given amount using the available coins by storing previously calculated results in a DP table. This avoids repeated calculations and makes the solution more efficient than a recursive approach.

Complexity
Time Complexity: O(N × M)
Space Complexity: O(N × M)